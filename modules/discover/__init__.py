"""Discover — intent-driven local discovery.

You tell LifeOS what you're after ("I'm in Lisbon this week, I want sushi night") and it
surfaces the PUBLIC events and crews that match. Your invite-only things — the local pool
evening — are never listed, no matter who is looking.

`core` is the pure matcher (filter + rank, no graph); `discover` is the graph flow.
"""
